import "./globals.css";

export const metadata = {
  title: "otp",
  description: "____",
};
export default function RootLayout({ children }) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
